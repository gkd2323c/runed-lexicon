# AGENTS.md

这是 `runed-lexicon` 的项目级 Agent 操作规范（常驻核心版）。

本文件只承载每次会话都必须强制生效的约束、生命周期与路由规则；完整工艺细则按关注点拆分子 skill，命中触发条件必须先完整读取对应 SKILL.md 再继续执行，严禁用摘要或历史记忆代行。

项目规则随翻译流程、脚本、数据结构、校验规则与协作方式的稳定而持续沉淀；一次性任务状态禁止写入本文件。

## 1. 项目目标

本项目借助 Agent / LLM 翻译《上古卷轴 V：天际》及其 MOD 文本。

当前已验证的主工作流：

`ESP / ESM -> xTranslator XML + xEdit 结构上下文 -> Agent 上下文构建 -> 翻译与审校结果 -> 确定性 XML 写回 -> xTranslator 导回插件`

Agent 的职责是恢复并利用任务、角色、对话、物品、地点、书籍和世界观上下文，在保证 XML 可安全导回的前提下，产出自然、一致、符合 TES 风格的中文。逐句机械直译不属于完成状态。

### 1.1 翻译价值基准：保真优先于顺口

专名与世界观概念的翻译，保真优先于顺口。社区通行叫法（雪漫、亚龙人、虎人、魔族/魔神、溪木镇等）常比官方形更顺口，本质是用玩家熟悉的旧认知框架消化 TES 的陌生概念；但顺口往往以消灭或变形 lore 为代价：

* **认知模板殖民**：Daedra 译为魔族，把超越善恶二元论的神学他者塞进“魔=邪恶”的既定模板，湮灭神学独特的他者性随之剥离。
* **伪谱系归类**：Argonian 译为亚龙人，虚构了与龙族的血缘联系（实为黑沼泽希斯特树的造物），在认知中植入伪设定。
* **多样性压扁**：Khajiit 译为虎人，抹平了其受双月相位决定、横跨近智人到四足兽类的庞大形态谱系。
* **剧透破坏**：Falmer 译为雪精灵，提前引爆随历史考察逐步揭示的种族蜕变悬念，破坏原版的叙事节奏。

判断与执行准则：

1. 官方译名是默认法理基准。音译保留异域感与神学留白；意译一旦出现偏差，便会将整个世界观钉死在误读之中。
2. 模型生成天然偏向通顺流畅，Agent 必须主动对抗这一先验：思考草稿出现社区俗称属于正常联想，但在落入正式译文前，必须通过 `dictionary/` 或 `global-forbidden-words.json` 校验回正。
3. **思维链降噪防内耗**：本准则的意义在于确立红线。在实际翻译条目的推理过程中，严禁在思考链内长篇大论复述上述哲学考证、名词的翻译或反复自我辩经；直接比对契约词表进行确定性判别，把注意力与算力留给当前句子的句法结构、角色语气与上下文还原。
4. **工具化防御边界**：`global-forbidden-words.json` 由自动化质量门禁以 TERM004 规则机械执行；对于未收录的新民间俗称，同样依保真原则予以拒绝，并同步沉淀到词库。

## 2. 目录约定与工作区卫生

### 2.0 目录拓扑与文件落点（硬性门禁）

* 根目录只承载项目级入口文档与固定目录；禁止存放临时脚本、查询日志、提取切片或中间报告。
* `.agents/skills/`：可复用的 Agent Skill 工具集、校验规则与执行脚本。
* `.work/<任务或MOD>/`：context JSON、翻译结果、批次状态与验证报告等过程资产。`.work/` 禁止作为临时测试代码的堆放地。
* `dictionary/`：Skyrim 官方英中 XML 语料库，作为官方译名的第一法理证据来源。
* `GLOBAL.md`：原版全局语境与世界观事实，由 `dictionary/` 归纳，禁止掺入特定 MOD 局部设定。
* `GLOSSARY.md`：跨 MOD 译法裁决记录，不收录单个 MOD 的独占术语。
* `global-forbidden-words.json`：全局禁用词库，经由 `term-contract-compiler` 编译注入各 MOD 契约。
* `mods/<plugin>/`：单个 MOD 工作目录，包含 `CONTEXT.md`、`DICTIONARY.md`、`PROGRESS.md`。

### 2.0.1 确定性输出契约（反文件繁殖，硬性门禁）

Agent 在任何环节输出的文件，语义、角色、路径三者必须确定：同一个角色永远只有同一个文件，重跑覆盖，禁止运行时即兴命名。

* **固定命名**：文件名由「角色 + 目标对象」构成，禁止日期戳、序号、`final`/`vN`/`roundN`/`backup`/`recheck`/`after-X` 等任何版本或状态标签。版本演进只允许存在于 `PROGRESS.md` 哈希链与写回报告中。
* **固定落点**：写回产物 = `mods/<plugin>/<plugin>_english_chinese_translated.xml`（唯一 canonical，同名覆盖式演进）；gate / noun-audit / writeback 报告 = `.work/<plugin>-gate-report.json` / `.work/<plugin>-noun-audit.json` / `.work/<plugin>-writeback-report.json`（同路径覆盖）；翻译 map 与结果 JSON 同理（角色名固定，如 `.work/<plugin>-fix-map.json`），同代次内多文件必须以角色语义区分（如 `-map-blockA`），不得以序号或时间戳区分。
* **历史演进**：上一代 canonical 进 `.work/<plugin>-archive/`；`PROGRESS.md` 记录哈希链。归档区文件保留原貌，不参与活跃工作流。
* **机械强制**：主流水线工具（writer / gate / noun-audit）对输出路径执行命名校验，不符合契约直接拒绝。agent 不得绕过（禁止用 shell 复制、重命名、重定向伪造产出路径）。
* **治理元规则**：本契约约束一切工作流环节与工具产出；新增工具或流程时若引入新的输出角色，必须在对应 SKILL.md 中登记固定路径后方可使用。工作流自身不符合本契约的，它本身就是治理对象。

### 2.1 执行卫生与异常阻断协议

* **零临时脚本**：全流程禁止在任何位置落盘一次性脚本。所有查询与处理必须调用现成 Skill 工具；无工具覆盖时必须先按 §A 走工具规范流程。
* **Shell 与命令确定性**：执行操作前确认当前终端宿主，显式声明工作目录。禁止跨平台混用语法（如将转义后的 `$null` 误作空设备）。严禁使用 shell 重定向直接覆盖正式译文。
* **阻断反馈协议**：遇到命令语法错误、重定向异常或门禁硬拦截时，立即中断操作，严禁输出冗长的自责或模糊推测，统一按以下结构化格式报告：

```text
[HALT / 门禁阻断]
- 阻断类型: <SHELL_ERROR GATE_BLOCKED MISSING_DOC |>
- 触发上下文: <报错命令 | 冲突词条与 REC ID | 缺失文件路径>
- 现场快照: <涉及的文件路径及当前状态>
- 待决选项: 
  1. <恢复或修复选项 A>
  2. <恢复或修复选项 B>
```

### 2.2 四文档与阶梯式加载生命周期（门禁）

为避免长会话早期一次性注入过多信息导致模型注意力衰减，工作流必须严格按以下三阶段顺序推进：

- **阶段 0：环境与跨 MOD 契约准备**

  读取根目录 `GLOBAL.md` 与 `GLOSSARY.md`。调用 `term-contract-compiler` 编译全局禁用词与跨 MOD 既定决策，形成当前工程的基础规则底座。

- **阶段 1：MOD 局部对齐与门禁核验**

  进入目标 MOD 前，全量精读该 MOD 目录下的 `CONTEXT.md` 与 `DICTIONARY.md`。若任一文件缺失，立即触发阻断并向用户确认是否补建；未获得明确决定前，不得静默创建，不得直接启动翻译。

- **阶段 2：批次翻译与动态技能调度**

  仅在实际进入特定批次的翻译或审校时，再强制读取 `skyrim-translation-craft`；在执行写回与校验前，再加载 `skyrim-xml-verification`。

### 2.3 局部特例豁免机制（白名单协议）

当 MOD 存在特殊设定（如特定角色因口音、认知偏差、戏谑双关故意使用民间叫法或错译形态）而必须突破全局禁用词时，必须走显式豁免通道：

1. 在该 MOD 的 `DICTIONARY.md` 中以固定语法声明：

  `[OVERRIDE_GLOBAL_BAN: <源词>] -> <特例译名> | 理由: <具体剧情或人物依据>`

2. 编译器将豁免条目打包进局部契约白名单；质量门禁在执行 TERM004/KEEP002 时优先校验白名单，命中有效豁免则放行通过，避免陷入规则死锁。

## 3. XML 安全约束

XML 结构完整性高于任何翻译层面的调整。修改仅限于目标字符串的 `<Dest>` 节点；所有写回操作必须通过 `xtranslator-xml-writer` 等确定性工具完成。

节点与格式红线：

- 绝对不修改 `<Source>`、`<EDID>`、`<REC>`、`<Params>` 元数据。

- 严禁增加、删除、截断、调换 `<String>` 节点。

- 保持原始排版、节点顺序、空白字符与换行特征；不把 XML 视作纯文本进行全局正则替换。

- 严格保留所有运行时占位符（`<Alias=...>`、`<Global=...>`、`%s`、`%d`、`{...}`、`[...]`、HTML/XML 实体控制符）。如遇无法断定是否可译的变量，一律保留原始形态。

## 4. 结果口径与真实验证

杜绝虚假完成，严格区分两种维度的验证状态：

- **工程验证通过**：代表 XML 结构完好、节点数量一致、占位符完整、自动化质量门禁（TERM/KEEP 规则）PASS。

- **语义翻译正确**：代表符合人物语域、Lore 传达准确、上下文顺畅。

- 实机验证状态由收口统一声明：各批次 handoff 禁止复读“未作游戏实机验证”（该事实对所有翻译卡相同，复读是噪音）；BOOK/整线收口报告（写回报告）中统一声明一次即可。推测性质的角色关系或说话者意图，必须显式标注“推测”。

## 5. 默认边界

- 原始 MOD XML 文件严禁就地覆盖，工具默认输出带标记的 translated XML。

- 用户要求完整推进 MOD 时以批次为单位流水线作业，不需要逐条停顿确认。

- 不初始化或擅自修改 Git 提交历史，除非用户发出明确指令。

- 修改中间结果 JSON 一律调用 `fill_translations.py`，查询分析优先调用现有封装工具。

## A. 规则技能与强制加载路由

当任务流程命中以下节点时，必须先全量读取对应技能的 `SKILL.md`，严禁凭记忆操作：

| **Skill**                       | **核心职责**                                                     | **强制加载时机**                           |
| ------------------------------- | ---------------------------------------------------------------- | ------------------------------------------ |
| `skyrim-translation-craft`      | 翻译风格、保真基准落地、语境通读、防剧透原则、各类 REC 处理策略  | 开始或审校翻译批次前；裁决歧义译名与语域时 |
| `skyrim-doc-system`             | 五文档职责划分、收录标准、术语状态生命周期与维护规范             | 创建或修改项目/MOD 文档前；划定术语归属时  |
| `skyrim-term-contract-workflow` | 契约编译工作流、门禁校验步骤、PASS 准则                          | 新 MOD 启动前；写回前；发布收敛性声明前    |
| `proper-noun-index`            | 官方专名索引构建与 MOD 源侧清点（人名/地名/派系名清单）        | 新 MOD 开工前清点官方名；裁决官方名词前；词典变更后重建索引 |
| `skyrim-xml-verification`       | 节点一致性校验、哈希比对、数据溯源                               | 批量修改 XML 前后；出具验证报告前          |
| `skyrim-tool-dev-rules`         | 工具复用检索、性能门禁（30s 缺陷阈值 / 5s 回归线）、禁止临时代码 | 编写数据处理脚本前；改动现有自动化工具前   |
| `skyrim-hana-collab`            | Hana 多 agent 协作编排：角色分工、并行扇出、试点放行、handoff 与失败恢复 | 需要并行翻译批次、用 subagent/workflow 派单、门禁 FAIL 打回原批次时 |

强制前置链：

新 MOD 启动流程：四文档门禁（§2.2 阶段 0~1） -> 编译术语契约（`skyrim-term-contract-workflow`） -> 批次翻译工艺（`skyrim-translation-craft`）。

需要数据操作时，首先检索 `.agents/skills/` 内的现成工具，坚决阻断重复造轮子。

## 6. 本文件维护原则

遇到以下情形时更新本文件或对应 Skill：

- 确认了新的确定性 XML 不变量或 xTranslator 行为特征。

- 建立了固化、通用的新自动化工作流，且已完成性能与稳定性回归。

- 发现了反复出现、需要依赖制度或程序强制防御的典型故障模式。

临时性 MOD 翻译决策、单一任务的临时记录以及未经验证的构想，禁止写入本规范。
